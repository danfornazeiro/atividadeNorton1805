spring:
  application:
    name: demo

  datasource:
    url: jdbc:postgresql://localhost:5432/atvNorton1805
    username: postgres
    password: Dy@515286
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update

    show-sql: true

    database-platform: org.hibernate.dialect.PostgreSQLDialect

  mail:
    host: smtp.gmail.com
    port: 587
    username: felipeacfornazeiro@gmail.com
    password: yqai xche mids qjcn

    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
            required: true

email:
  subject: "Default Subject"
  message: "Default Message"